api.SteerToLeft();

var frontDistance = api.GetDistanceToNearestObstacle(0);

frontDistance = Mathf.Min(100, frontDistance);

if (frontDistance < 0) {
	frontDistance = 100;
}


if (api.GetSpeed() > 25) { 
	api.SetThrottle(0f);
} else {
	api.SetThrottle(70f);
}