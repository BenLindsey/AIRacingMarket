var maxSpeed = 25f;
var dist = Mathf.Max(api.GetDistanceToEdge(0), api.GetDistanceToCar(0));

if (dist < 50) {
	maxSpeed = 20f;
}

api.SteerToLeft();

if (api.GetSpeed() > maxSpeed) { 
	api.SetThrottle(0f);
} else {
	api.SetThrottle(65f);
}