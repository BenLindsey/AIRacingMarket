var Init = function() {
	data["speed"] = 20;
	Debug.Log(data);
};

var PhysicsUpdate = function() {
	api.SteerToLeft();

	data["speed"] = data["speed"] - 0.01;

	Debug.Log(data["speed"]);

	if (api.GetSpeed() > data["speed"]) { 
		api.SetThrottle(0f);
	} else {
		api.SetThrottle(70f);
	}
};